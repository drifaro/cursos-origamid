// Declare uma variável contendo uma string
var jogoPreferido = 'Valorant';
console.log(jogoPreferido, typeof jogoPreferido);

// Declare uma variável contendo um número dentro de uma string
var dataNascimento = "2 de julho de 1995";
console.log(dataNascimento, typeof dataNascimento);
// Declare uma variável com a sua idade
var idade = 26;
console.log(idade, typeof idade);

// Declare duas variáveis, uma com seu nome
var nome = 'Drislaine';
// e outra com seu sobrenome e some as mesmas
var sobrenome = 'Faro';

var nomeCompleto = nome + ' ' + sobrenome;
console.log(nomeCompleto, typeof nomeCompleto);
var nomeCompleto = `${nome} ${sobrenome}`;
console.log(nomeCompleto, typeof nomeCompleto);


// Coloque a seguinte frase em uma variável: It's time
var frase = "It's time";
console.log(frase);
var frase = 'It\'s time';
console.log(frase);
var frase = `It's time`;
console.log(frase);

// Verifique o tipo da variável que contém o seu nome
console.log(typeof nome);
